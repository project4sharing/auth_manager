[project]
name = "auth-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk[agent-identity,extensions]>=2.4.0",
    "google-cloud-aiplatform[adk,agent-engines]>=1.156.0",
    "httpx>=0.28.1",
]
